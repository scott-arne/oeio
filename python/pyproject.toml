[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "oeio"
version = "0.2.3"
requires-python = ">=3.10"
dependencies = [
    "openeye-toolkits>=2025.2",
]

[tool.setuptools]
packages = ["oeio"]
