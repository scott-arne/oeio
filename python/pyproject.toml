[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "oeio"
version = "0.6.0"
requires-python = ">=3.11"
dependencies = [
    "openeye-toolkits>=2026.1",
]

[tool.setuptools]
packages = ["oeio"]
